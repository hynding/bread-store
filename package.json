{
  "name": "bread-store",
  "version": "0.5.0",
  "description": "A BREAD (Browse, Read, Edit, Add, Destroy) -ed store class for persistent storage models.",
  "main": "index.js",
  "scripts": {
    "build-local-continuous": "babel src --watch -d dist",
    "build-local": "babel src -d dist",
    "feature-test": "cucumberjs --compiler js:babel-core/register",
    "prefeature-test": "npm run build-local",
    "prepublish": "npm run build-local",
    "test": "npm run feature-test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hynding/bread-store.git"
  },
  "author": "Steve Hynding",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/hynding/bread-store/issues"
  },
  "homepage": "https://github.com/hynding/bread-store#readme",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.4.0",
    "cucumber": "^2.0.0-rc.9"
  }
}
